# Root Directory on Railway = api
[build]
builder = "NIXPACKS"
buildCommand = "(npm ci || npm install) && npx prisma generate && npm run build"

[deploy]
startCommand = "node start.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
