# Root Directory on Railway = web
[build]
builder = "NIXPACKS"
buildCommand = "(npm ci || npm install) && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
